pandas
numpy
scipy
scikit-learn
nba_api
archetypes
matplotlib
seaborn
jupyter
requests
lxml
rich
streamlit
plotly
pyarrow
kaleido
jinja2
playwright  # browser binary itself comes from packages.txt's `chromium` (see player_report.py's launch fallback), not `playwright install chromium` - Streamlit Community Cloud has no post-pip-install hook to run that command
pdf2image  # also needs poppler installed system-wide (pdftoppm/pdftocairo on PATH)